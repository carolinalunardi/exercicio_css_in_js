import { FormContainer, FormHero, HeroTitle } from './styles'

const Hero = () => (
  <FormHero>
    <FormContainer>
      <HeroTitle>
        As melhores vagas para tecnologia, design e artes visuais.
      </HeroTitle>
    </FormContainer>
  </FormHero>
)

export default Hero
